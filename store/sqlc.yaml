version: "2"
sql:
  - engine: "sqlite"
    schema: "schema/migrations"
    queries: "schema/queries"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
